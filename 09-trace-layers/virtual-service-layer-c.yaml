apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: layer-c
spec:
  hosts:
  - layer-c
  http:
  - match:
    - uri:
        exact: /call-layers
    - uri:
        exact: /get-info       
    - uri:
        exact: /get-json                
    - uri:
        exact: /
    route:
    - destination:
        host: layer-c
        port:
          number: 8080