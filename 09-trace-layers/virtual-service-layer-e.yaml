apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: layer-e
spec:
  hosts:
  - layer-e
  http:
  - match:
    - uri:
        exact: /call-layers
    - uri:
        exact: /get-info       
    - uri:
        exact: /get-json                
    - uri:
        exact: /
    route:
    - destination:
        host: layer-e
        port:
          number: 8080