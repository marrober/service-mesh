apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: layer1-gateway
spec:
  selector:
     istio: service-mesh-01-gw 
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - "*"