apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: pebbles
spec:
  hosts:
  - pebbles-flintstones.apps.cluster-c2d5.c2d5.example.opentlc.com
  gateways:
  - flintstones-gateway-pebbles
  http:
  - match:
    - uri:
        exact: /call-layers
    - uri:
        exact: /get-info        
    - uri:
        exact: /
    route:
    - destination:
        host: pebbles # interpreted as wilma.foo.svc.cluster.local
        port:
          number: 8080